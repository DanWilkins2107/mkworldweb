{
  "name": "mkworldweb",
  "private": true,
  "scripts": {
    "build": "npm --prefix app run build",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
